/**
 * Vercel Express entry at the repository root so deployment can use Root Directory "."
 * @see https://vercel.com/docs/frameworks/backend/express
 */
export { default } from './backend/index.js';
